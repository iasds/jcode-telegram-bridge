[Unit]
Description=jcode Harness API bridge (SDK unix socket)

[Service]
StartLimitIntervalSec=120
ExecStart=/root/.local/bin/jcode api-bridge
Restart=always
RestartSec=3
Environment=JCODE_RUNTIME_DIR=/run/user/0
Environment=PATH=/root/.local/bin:/usr/bin:/bin

[Install]
WantedBy=multi-user.target
